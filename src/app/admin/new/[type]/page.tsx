import { notFound } from 'next/navigation'; import { AdminShell } from '@/components/AdminShell'; import { ContentEditorClient } from '@/components/ContentEditorClient'; import { requireAdminPage } from '@/lib/admin-page'; import { allContentTypes } from '@/lib/content-fields'; import type { ContentType } from '@/lib/types';
export default async function Page({params}:{params:Promise<{type:string}>}){const user=await requireAdminPage();const{type}=await params;if(!allContentTypes.includes(type as ContentType))notFound();return <AdminShell user={user}><ContentEditorClient type={type as ContentType}/></AdminShell>}
