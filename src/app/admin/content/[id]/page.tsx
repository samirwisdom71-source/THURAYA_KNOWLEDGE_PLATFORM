import { notFound } from 'next/navigation'; import { AdminShell } from '@/components/AdminShell'; import { ContentEditorClient } from '@/components/ContentEditorClient'; import { requireAdminPage } from '@/lib/admin-page'; import { query } from '@/lib/db'; import type { ContentItem } from '@/lib/types';
export default async function Page({params}:{params:Promise<{id:string}>}){const user=await requireAdminPage();const{id}=await params;const r=await query<ContentItem>('SELECT * FROM content_items WHERE id=$1 LIMIT 1',[id]);const item=r.rows[0];if(!item)notFound();return <AdminShell user={user}><ContentEditorClient item={item} type={item.content_type}/></AdminShell>}
