import { notFound } from 'next/navigation';
import { getSetting } from '@/lib/content'; import { isLocale } from '@/lib/locale'; import { Markdown } from '@/lib/markdown'; import { MediaVisual } from '@/components/MediaVisual';
export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const [ar,en,asset]=await Promise.all([getSetting('about_page_markdown_ar',''),getSetting('about_page_markdown_en',''),getSetting<string|null>('about_asset_id',null)]);const text=locale==='en'&&en?String(en):String(ar);return <><section className="pageHero"><div className="container"><span className="kicker">ABOUT THURAYA</span><h1>{locale==='ar'?'المعرفة تصبح أكثر قيمة عندما تتحول إلى أثر':'Knowledge becomes more valuable when it turns into impact'}</h1><p>{locale==='ar'?'عن الاهتمامات والأفكار التي تقف خلف هذه المنصة الشخصية.':'The interests and ideas behind this personal platform.'}</p></div></section><section className="section"><div className="container contentLayout"><article className="articlePanel"><Markdown text={text}/></article><aside><MediaVisual assetId={asset} alt={locale==='ar'?'ثريا الشامسي':'Thuraya Al Shamsi'}/></aside></div></section></>}
