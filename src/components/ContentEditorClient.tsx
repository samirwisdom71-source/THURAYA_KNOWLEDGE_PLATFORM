'use client';
import { useEffect,useState } from 'react';
import { useRouter } from 'next/navigation';
import { IconButton } from '@/components/AdminIcon';
import type { ContentItem, ContentType, FieldSpec, MediaAsset } from '@/lib/types';
import { contentFields, contentTypeLabels } from '@/lib/content-fields';

function toInput(v:unknown,t:FieldSpec['type']){if(t==='array')return Array.isArray(v)?v.join('\n'):'';if(t==='boolean')return Boolean(v);return v==null?'':String(v)}
function fromInput(v:unknown,t:FieldSpec['type']){if(t==='array')return String(v).split('\n').map(x=>x.trim()).filter(Boolean);if(t==='number')return v===''?null:Number(v);if(t==='boolean')return Boolean(v);return String(v??'')}

export function ContentEditorClient({item,type}:{item?:ContentItem;type:ContentType}){
  const router=useRouter();
  const[tab,setTab]=useState<'ar'|'en'>('ar'); const[status,setStatus]=useState(item?.status||'draft'); const[visibility,setVisibility]=useState(item?.visibility||'public'); const[showPublishDate,setShowPublishDate]=useState(item?.show_publish_date||false); const[slug,setSlug]=useState(item?.slug||'');
  const[dataAr,setDataAr]=useState<Record<string,unknown>>(item?.data_ar||{}); const[dataEn,setDataEn]=useState<Record<string,unknown>>(item?.data_en||{}); const[manualOverrideEn,setManualOverrideEn]=useState(Boolean(item?.manual_override_en));
  const[privateData,setPrivateData]=useState(JSON.stringify(item?.private_data||{},null,2)); const[msg,setMsg]=useState(''); const[saving,setSaving]=useState(false); const[media,setMedia]=useState<MediaAsset[]>([]); const[archiveArmed,setArchiveArmed]=useState(false);
  useEffect(()=>{fetch('/api/admin/media?limit=200').then(r=>r.json()).then(j=>setMedia(j.items||[])).catch(()=>{})},[]);
  const fields=contentFields[type]; const data=tab==='ar'?dataAr:dataEn;
  function setField(spec:FieldSpec,value:unknown){const next={...data,[spec.key]:fromInput(value,spec.type)};if(tab==='ar')setDataAr(next);else{setDataEn(next);setManualOverrideEn(true)}}
  async function save(nextStatus=status){setSaving(true);setMsg('');let parsedPrivate={};try{parsedPrivate=privateData?JSON.parse(privateData):{}}catch{setMsg('JSON الحقول الداخلية غير صالح');setSaving(false);return;}const body={contentType:type,slug,status:nextStatus,visibility,showPublishDate,dataAr,dataEn,privateData:parsedPrivate,manualOverrideEn};const url=item?`/api/admin/content/${item.id}`:'/api/admin/content';const r=await fetch(url,{method:item?'PATCH':'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const j=await r.json().catch(()=>({}));setSaving(false);if(!r.ok){setMsg(j.error||'تعذر الحفظ');return;}setStatus(j.item?.status||nextStatus);setMsg('تم الحفظ بنجاح');if(!item&&j.item?.id)router.replace(`/admin/content/${j.item.id}`);router.refresh()}
  async function translate(){if(!item){setMsg('احفظ السجل أولًا ثم اطلب الترجمة.');return;}setSaving(true);setMsg('');const r=await fetch(`/api/admin/content/${item.id}/translate`,{method:'POST'});const j=await r.json().catch(()=>({}));setSaving(false);if(!r.ok){setMsg(j.error||'تعذر الترجمة');return;}setDataEn(j.item.data_en||{});setManualOverrideEn(false);setTab('en');setMsg('تم إنشاء الترجمة الآلية. أي تعديل يدوي على تبويب English سيُثبت كـ manual override.')}
  async function archive(){if(!item)return;if(!archiveArmed){setArchiveArmed(true);setMsg('اضغطي أيقونة الأرشفة مرة ثانية لنقل السجل إلى الأرشيف وإخفائه عن العامة.');return;}setSaving(true);const r=await fetch(`/api/admin/content/${item.id}`,{method:'DELETE'});const j=await r.json().catch(()=>({}));setSaving(false);if(!r.ok){setMsg(j.error||'تعذر الأرشفة');return;}router.replace('/admin/content');router.refresh()}
  return <div className="adminEditor"><section className="editorPanel"><div className="adminToolbar"><div><span className="kicker">{contentTypeLabels[type].ar}</span><h2 style={{margin:0}}>{item?'تعديل المحتوى':'محتوى جديد'}</h2></div><div className="iconRow editorActions"><IconButton name="save" label={saving?'جارٍ الحفظ':'حفظ'} onClick={()=>save()} disabled={saving}/><IconButton name="send" label="حفظ ونشر" tone="gold" onClick={()=>save('published')} disabled={saving}/>{item&&<IconButton name="globe" label="ترجمة للإنجليزية" onClick={translate} disabled={saving}/>}{item&&<IconButton name="trash" label={archiveArmed?'تأكيد الأرشفة':'أرشفة'} tone="danger" onClick={archive} disabled={saving}/>}</div></div>{msg&&<div className="formMessage">{msg}</div>}<div className="tabRow"><button className={`tabButton ${tab==='ar'?'active':''}`} onClick={()=>setTab('ar')}>العربية</button><button className={`tabButton ${tab==='en'?'active':''}`} onClick={()=>setTab('en')}>English {manualOverrideEn?'• يدوي':''}</button></div><div className="fieldGrid">{fields.map(spec=><div className={`field ${['textarea','markdown','array'].includes(spec.type)?'wide':''}`} key={spec.key}><label>{tab==='ar'?spec.labelAr:spec.labelEn}{spec.required?' *':''}</label>{spec.type==='boolean'?<label className="check"><input type="checkbox" checked={Boolean(data[spec.key])} onChange={e=>setField(spec,e.target.checked)}/><span>{Boolean(data[spec.key])?'نعم':'لا'}</span></label>:spec.type==='asset'?<select value={String(data[spec.key]||'')} onChange={e=>setField(spec,e.target.value)}><option value="">— بدون ملف —</option>{media.map(m=><option key={m.id} value={m.id}>{m.original_name} {m.public_safe_review&&m.visibility==='public'?'✓ عام ومراجع':'(خاص/غير مراجع)'}</option>)}</select>:spec.type==='textarea'||spec.type==='markdown'||spec.type==='array'?<textarea rows={spec.type==='markdown'?12:5} value={String(toInput(data[spec.key],spec.type))} onChange={e=>setField(spec,e.target.value)}/>:<input type={spec.type==='number'?'number':spec.type==='url'?'url':'text'} value={String(toInput(data[spec.key],spec.type))} onChange={e=>setField(spec,e.target.value)}/>}</div>)}</div></section><aside className="editorPanel"><h3>إعدادات النشر</h3><div className="field"><label>Slug</label><input dir="ltr" value={slug} onChange={e=>setSlug(e.target.value)}/></div><div className="field"><label>الحالة</label><select value={status} onChange={e=>setStatus(e.target.value as typeof status)}><option value="draft">مسودة</option><option value="ready">جاهز</option><option value="published">منشور</option><option value="archived">مؤرشف</option><option value="awaiting_image">بانتظار صورة</option></select></div><div className="field"><label>الظهور</label><select value={visibility} onChange={e=>setVisibility(e.target.value as typeof visibility)}><option value="public">عام</option><option value="private">خاص</option><option value="unlisted">غير مفهرس</option></select></div><label className="check"><input type="checkbox" checked={showPublishDate} onChange={e=>setShowPublishDate(e.target.checked)}/><span>إظهار تاريخ أول نشر الحقيقي للعامة</span></label>{item?.first_published_at&&<p className="muted">أول نشر: {new Date(item.first_published_at).toLocaleString('ar-AE')} — لا يمكن تعديله يدويًا.</p>}<label className="check"><input type="checkbox" checked={manualOverrideEn} onChange={e=>setManualOverrideEn(e.target.checked)}/><span>تثبيت الإنجليزية كتعديل يدوي وعدم استبدالها آليًا</span></label><h3>حقول داخلية</h3><p className="muted">لا تخرج للعامة أو الـAPI العام.</p><textarea className="jsonPrivate" value={privateData} onChange={e=>setPrivateData(e.target.value)}/></aside></div>;
}
